<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
  	<meta charset="utf-8" />

  	<!-- Uncomment to make IE8 render like IE7 -->
  	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

  	<!-- Set the viewport width to device width for mobile -->
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

  	<title><%= content_for?(:title) ? yield(:title) : "Weblog" %></title>

  	<%= stylesheet_link_tag    "application" %>
  	<%= javascript_include_tag "vendor/custom.modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="container">
      <div class="row">
        <div class="large-12 columns">
          <h1><%= link_to "Weblog", root_path, style:"color:#000" %> <small>ruby on rails demo</small></h1>
          <% if user_signed_in? %>
            <span>
              Hi <%= current_user.email %> 
              <%= link_to "Edit Profile", edit_user_registration_path, class:"small button" %> 
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"small alert button" %>
            </span>
          <% else %>
              <%= link_to "Login", new_user_session_path %> or 
              <%= link_to "Register", new_user_registration_path %>
          <% end %>
        </div>
      </div>

      <hr />

      <% if notice %>
        <div class="row">
          <div class="large-12 columns"> <span style="color:green"><%= notice %></span> </div>
        </div>
      <% end %>

      <% if alert %>
        <div class="row">
          <div class="large-12 columns"> <span style="color:red"><%= alert %></span> </div>
        </div>
      <% end %>

      <%= yield %>

      <div class="row" style="margin-top:20px">
        <div class="large-12 columns">
          
        </div>
      </div>

    </div>

    
    <%= javascript_include_tag "application" %>
  </body>
</html>
